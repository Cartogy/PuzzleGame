image: barichello/godot-ci:3.2.3

# Cache imported assets between runs
#cache:
# key: import-assets
#  paths:
#    - .import/

stages:
  - tests
    #- export
    #  - deploy

variables:
  EXPORT_NAME: test-project


unitTests:
        stage: tests
        script:
                - cd Godot/Project
                - godot -s addons/gut/gut_cmdln.gd -d --path $PWD -ginclude-subdirs -gdir=res://test -gexit -gprefix=test_
